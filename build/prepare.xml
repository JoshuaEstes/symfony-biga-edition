<project>
    <target name="prepare" description="Prepare for the build." depends="clean">
        <mkdir dir="${project.build_dir}/api" />
        <mkdir dir="${project.build_dir}/logs" />
        <mkdir dir="${project.build_dir}/pdepend" />
    </target>
</project>
